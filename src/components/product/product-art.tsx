/**
 * Deprecated.
 *
 * This file held the generated SVG placeholder artwork used before the real ZENJI
 * photography was wired in. Product images now come from
 * `components/product/product-image.tsx`, backed by the helpers in `lib/products.ts`.
 *
 * Nothing imports this module. It is safe to delete.
 */

export {};
