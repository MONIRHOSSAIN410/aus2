# dependencies
node_modules
.pnp
.pnp.js

# builds
.next
out
build

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env*.local

# deploy / editor
.vercel
.idea
.vscode

# generated
next-env.d.ts
*.tsbuildinfo

# scratch copies — a stray duplicate of the app in the repo gets type-checked
# and linted by the build, which is a confusing way to fail a deploy
_project/
_project.zip
*.zip
